# Variáveis de ambiente para desenvolvimento local
# Copie este arquivo para `.env.local` e ajuste conforme necessário.

# Porta padrão do servidor de desenvolvimento/produção local
PORT=3001

# Host para bind do servidor (opcional)
# HOST=0.0.0.0

